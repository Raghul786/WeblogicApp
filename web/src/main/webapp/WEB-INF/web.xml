<?xml version="1.0" encoding="UTF-8"?>
<web-app
    xmlns="http://xmlns.jcp.org/xml/ns/javaee"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://xmlns.jcp.org/xml/ns/javaee
        http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
    version="3.1">

    <display-name>Reddit App</display-name>

    <!-- ===================== -->
    <!-- User Count -->
    <!-- ===================== -->

    <servlet>
        <servlet-name>UserCountServlet</servlet-name>
        <servlet-class>
            com.example.reddit.web.servlet.UserCountServlet
        </servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>UserCountServlet</servlet-name>
        <url-pattern>/reddit/users/count</url-pattern>
    </servlet-mapping>

    <!-- ===================== -->
    <!-- Health -->
    <!-- ===================== -->

    <servlet>
        <servlet-name>HealthServlet</servlet-name>
        <servlet-class>
            com.example.reddit.web.servlet.HealthServlet
        </servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>HealthServlet</servlet-name>
        <url-pattern>/health</url-pattern>
    </servlet-mapping>

    <!-- ===================== -->
    <!-- Login -->
    <!-- ===================== -->

    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>
            com.example.reddit.web.servlet.LoginServlet
        </servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/login</url-pattern>
    </servlet-mapping>

    <!-- ===================== -->
    <!-- Logout -->
    <!-- ===================== -->

    <servlet>
        <servlet-name>LogoutServlet</servlet-name>
        <servlet-class>
            com.example.reddit.web.servlet.LogoutServlet
        </servlet-class>
    </servlet>

    <servlet-mapping>
        <servlet-name>LogoutServlet</servlet-name>
        <url-pattern>/logout</url-pattern>
    </servlet-mapping>

    <!-- ===================== -->
    <!-- Datasource -->
    <!-- ===================== -->

    <resource-ref>
        <res-ref-name>jdbc/RedditDS</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
    </resource-ref>

    <!-- ===================== -->
    <!-- Auth Filter -->
    <!-- ===================== -->

    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>
            com.example.reddit.web.filter.AuthFilter
        </filter-class>
    </filter>

    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/reddit/*</url-pattern>
    </filter-mapping>
    
    <session-config>
        <session-timeout>2</session-timeout>
</session-config>

<servlet>
    <servlet-name>FeedServlet</servlet-name>
    <servlet-class>
        com.example.reddit.web.servlet.FeedServlet
    </servlet-class>
</servlet>

<servlet-mapping>
    <servlet-name>FeedServlet</servlet-name>
    <url-pattern>/reddit/feed</url-pattern>
</servlet-mapping>
<servlet>
    <servlet-name>VoteServlet</servlet-name>
    <servlet-class>
        com.example.reddit.web.servlet.VoteServlet
    </servlet-class>
</servlet>

<servlet-mapping>
    <servlet-name>VoteServlet</servlet-name>
    <url-pattern>/reddit/vote</url-pattern>
</servlet-mapping>


</web-app>

